---
/**
 * Juggling pattern card backed by a pre-rendered animated GIF.
 *
 * Unlike the canvas-based JugglingPattern.astro (which simulates throws live),
 * this component shows a JugglingLab-rendered GIF of Linda's stick figure
 * running the pattern. GIFs live at /animations/pattern-{slug}.gif and are
 * loaded lazily so an out-of-viewport card costs nothing.
 */
interface Props {
  src: string;
  title: string;
  notation: string;
  description: string;
  difficulty?: "beginner" | "intermediate" | "advanced";
  pillar?: "brain" | "tech" | "change" | "all";
}

const {
  src,
  title,
  notation,
  description,
  difficulty = "beginner",
  pillar = "all",
} = Astro.props;

const pillarColor: Record<string, string> = {
  brain: "var(--color-brain)",
  tech: "var(--color-tech)",
  change: "var(--color-change)",
  all: "var(--color-tech-change)",
};
const accent = pillarColor[pillar];

const difficultyLabel: Record<string, string> = {
  beginner: "Beginner",
  intermediate: "Intermediate",
  advanced: "Advanced",
};
const difficultyDots = { beginner: 1, intermediate: 2, advanced: 3 }[difficulty];
---

<article class="pattern-gif-card group" style={`--accent: ${accent};`}>
  <div class="img-wrap">
    <img
      src={src}
      alt={`Animated juggling pattern: ${title}, siteswap ${notation}`}
      loading="lazy"
      decoding="async"
      class="pattern-gif"
    />
    <div class="glow-overlay" aria-hidden="true"></div>

    <span class="difficulty-pill" aria-label={`Difficulty: ${difficultyLabel[difficulty]}`}>
      <span class="dots" aria-hidden="true">
        {Array.from({ length: 3 }, (_, i) => (
          <span class:list={["dot", { on: i < difficultyDots }]}></span>
        ))}
      </span>
      {difficultyLabel[difficulty]}
    </span>
  </div>

  <div class="card-body">
    <p class="siteswap" aria-label={`Siteswap notation: ${notation}`}>
      Siteswap {notation}
    </p>
    <h3 class="card-title">{title}</h3>
    <p class="card-desc">{description}</p>
  </div>
</article>

<style>
  .pattern-gif-card {
    display: flex;
    flex-direction: column;
    border-radius: 1.25rem;
    border: 1px solid color-mix(in oklab, var(--accent) 25%, var(--color-border));
    background: var(--color-bg-card);
    overflow: hidden;
    transition: box-shadow 240ms ease, transform 240ms ease, border-color 240ms ease;
  }
  .pattern-gif-card:hover {
    border-color: color-mix(in oklab, var(--accent) 60%, var(--color-border));
    box-shadow:
      0 0 0 1px color-mix(in oklab, var(--accent) 20%, transparent),
      0 8px 32px -8px color-mix(in oklab, var(--accent) 35%, transparent),
      0 24px 64px -16px color-mix(in oklab, var(--accent) 15%, transparent);
    transform: translateY(-3px);
  }

  .img-wrap {
    position: relative;
    background: linear-gradient(180deg, #080810 0%, #0d0d1a 100%);
    aspect-ratio: 1 / 1;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .pattern-gif {
    width: 100%;
    height: 100%;
    object-fit: contain;
    display: block;
    position: relative;
    z-index: 2;
  }

  .glow-overlay {
    position: absolute;
    inset: 0;
    z-index: 1;
    background: radial-gradient(
      ellipse 70% 60% at 50% 50%,
      color-mix(in oklab, var(--accent) 8%, transparent),
      transparent 70%
    );
    pointer-events: none;
  }

  .difficulty-pill {
    position: absolute;
    top: 0.75rem;
    right: 0.75rem;
    z-index: 3;
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.25rem 0.6rem;
    border-radius: 999px;
    background: color-mix(in oklab, var(--color-bg) 70%, transparent);
    backdrop-filter: blur(6px);
    border: 1px solid color-mix(in oklab, var(--accent) 35%, var(--color-border));
    font-size: 0.65rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--color-fg);
  }
  .dots {
    display: inline-flex;
    gap: 2px;
  }
  .dot {
    width: 5px;
    height: 5px;
    border-radius: 999px;
    background: color-mix(in oklab, var(--accent) 18%, var(--color-border));
  }
  .dot.on {
    background: var(--accent);
    box-shadow: 0 0 6px color-mix(in oklab, var(--accent) 60%, transparent);
  }

  .card-body {
    padding: 1.1rem 1.25rem 1.25rem;
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
    flex: 1;
  }
  .siteswap {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--accent);
    opacity: 0.85;
  }
  .card-title {
    font-family: var(--font-display);
    font-size: 1.15rem;
    font-weight: 600;
    color: var(--color-fg);
    line-height: 1.2;
  }
  .card-desc {
    font-size: 0.875rem;
    line-height: 1.55;
    color: var(--color-muted);
    margin-top: 0.15rem;
  }
</style>
